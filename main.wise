Use ConsoleX64

import "kernel32.dll":
    Sleep(int ms) -> void

import "user32.dll":
    MessageBoxA(int hwnd, string text, string caption, int flags) -> int

init main:
    int delay = 2000
    print [delay]
    call Sleep([delay])
    call MessageBoxA(0, "Hi", "T", 0)